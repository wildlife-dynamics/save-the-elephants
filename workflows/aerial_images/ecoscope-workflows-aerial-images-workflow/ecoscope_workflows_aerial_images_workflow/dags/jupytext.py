# AUTOGENERATED BY ECOSCOPE-WORKFLOWS; see fingerprint in README.md for details


# ruff: noqa: E402

# %% [markdown]
# # Aerial Images
# TODO: top level description

# %% [markdown]
# ## Imports

import os

from ecoscope_workflows_core.tasks.config import (
    set_workflow_details as set_workflow_details,
)
from ecoscope_workflows_core.tasks.filter import (
    get_timezone_from_time_range as get_timezone_from_time_range,
)
from ecoscope_workflows_core.tasks.filter import set_time_range as set_time_range
from ecoscope_workflows_core.tasks.io import persist_text as persist_text
from ecoscope_workflows_core.tasks.io import set_er_connection as set_er_connection
from ecoscope_workflows_core.tasks.results import (
    create_plot_widget_single_view as create_plot_widget_single_view,
)
from ecoscope_workflows_core.tasks.results import gather_dashboard as gather_dashboard
from ecoscope_workflows_core.tasks.skip import (
    any_dependency_skipped as any_dependency_skipped,
)
from ecoscope_workflows_core.tasks.skip import any_is_empty_df as any_is_empty_df
from ecoscope_workflows_core.tasks.skip import never as never
from ecoscope_workflows_core.tasks.transformation import (
    convert_values_to_timezone as convert_values_to_timezone,
)
from ecoscope_workflows_ext_ecoscope.tasks.io import get_events as get_events
from ecoscope_workflows_ext_ecoscope.tasks.io import persist_df as persist_df
from ecoscope_workflows_ext_ste.tasks import build_matched_table as build_matched_table
from ecoscope_workflows_ext_ste.tasks import (
    build_unmatched_table as build_unmatched_table,
)
from ecoscope_workflows_ext_ste.tasks import (
    get_unmatched_images as get_unmatched_images,
)
from ecoscope_workflows_ext_ste.tasks import (
    match_images_to_events as match_images_to_events,
)
from ecoscope_workflows_ext_ste.tasks import (
    process_aerial_images as process_aerial_images,
)
from ecoscope_workflows_ext_ste.tasks import (
    upload_images_to_er_events as upload_images_to_er_events,
)

# %% [markdown]
# ## Set Workflow Details

# %%
# parameters

workflow_details_params = dict(
    name=...,
    description=...,
    image_url=...,
)

# %%
# call the task


workflow_details = (
    set_workflow_details.set_task_instance_id("workflow_details")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**workflow_details_params)
    .call()
)


# %% [markdown]
# ## Time Range

# %%
# parameters

time_range_params = dict(
    since=...,
    until=...,
    timezone=...,
    time_format=...,
)

# %%
# call the task


time_range = (
    set_time_range.set_task_instance_id("time_range")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**time_range_params)
    .call()
)


# %% [markdown]
# ## Data Source

# %%
# parameters

er_client_name_params = dict(
    data_source=...,
)

# %%
# call the task


er_client_name = (
    set_er_connection.set_task_instance_id("er_client_name")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**er_client_name_params)
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

get_timezone_params = dict()

# %%
# call the task


get_timezone = (
    get_timezone_from_time_range.set_task_instance_id("get_timezone")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(time_range=time_range, **get_timezone_params)
    .call()
)


# %% [markdown]
# ## Process Aerial Images

# %%
# parameters

images_data_params = dict(
    image_folder=...,
)

# %%
# call the task


images_data = (
    process_aerial_images.set_task_instance_id("images_data")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(timezone=get_timezone, **images_data_params)
    .call()
)


# %% [markdown]
# ## Get Events

# %%
# parameters

events_data_params = dict(
    event_types=...,
    include_updates=...,
    include_related_events=...,
)

# %%
# call the task


events_data = (
    get_events.set_task_instance_id("events_data")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        client=er_client_name,
        time_range=time_range,
        event_columns=[
            "id",
            "time",
            "created_at",
            "serial_number",
            "event_type",
            "event_category",
            "geometry",
        ],
        include_details=False,
        include_null_geometry=True,
        include_display_values=True,
        raise_on_empty=False,
        **events_data_params,
    )
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

convert_events_to_user_tz_params = dict()

# %%
# call the task


convert_events_to_user_tz = (
    convert_values_to_timezone.set_task_instance_id("convert_events_to_user_tz")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=events_data,
        timezone=get_timezone,
        columns=["time"],
        **convert_events_to_user_tz_params,
    )
    .call()
)


# %% [markdown]
# ## Attach Images to Events

# %%
# parameters

matched_data_params = dict(
    time_window_minutes=...,
)

# %%
# call the task


matched_data = (
    match_images_to_events.set_task_instance_id("matched_data")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        images_df=images_data,
        events_df=convert_events_to_user_tz,
        **matched_data_params,
    )
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

unmatched_data_params = dict()

# %%
# call the task


unmatched_data = (
    get_unmatched_images.set_task_instance_id("unmatched_data")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(images_df=images_data, matched_df=matched_data, **unmatched_data_params)
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

persist_matched_csv_params = dict()

# %%
# call the task


persist_matched_csv = (
    persist_df.set_task_instance_id("persist_matched_csv")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=matched_data,
        filetype="csv",
        filename="matched_images",
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_matched_csv_params,
    )
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

persist_unmatched_csv_params = dict()

# %%
# call the task


persist_unmatched_csv = (
    persist_df.set_task_instance_id("persist_unmatched_csv")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=unmatched_data,
        filetype="csv",
        filename="unmatched_images",
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_unmatched_csv_params,
    )
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

matched_html_params = dict()

# %%
# call the task


matched_html = (
    build_matched_table.set_task_instance_id("matched_html")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(matched_df=matched_data, **matched_html_params)
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

persist_matched_html_params = dict(
    filename_suffix=...,
)

# %%
# call the task


persist_matched_html = (
    persist_text.set_task_instance_id("persist_matched_html")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=matched_html,
        filename="matched_images_preview.html",
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_matched_html_params,
    )
    .call()
)


# %% [markdown]
# ## Matched Images

# %%
# parameters

matched_widget_params = dict(
    view=...,
)

# %%
# call the task


matched_widget = (
    create_plot_widget_single_view.set_task_instance_id("matched_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Matched Images", data=persist_matched_html, **matched_widget_params)
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

unmatched_html_params = dict()

# %%
# call the task


unmatched_html = (
    build_unmatched_table.set_task_instance_id("unmatched_html")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(unmatched_df=unmatched_data, **unmatched_html_params)
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

persist_unmatched_html_params = dict(
    filename_suffix=...,
)

# %%
# call the task


persist_unmatched_html = (
    persist_text.set_task_instance_id("persist_unmatched_html")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=unmatched_html,
        filename="unmatched_images_preview.html",
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_unmatched_html_params,
    )
    .call()
)


# %% [markdown]
# ## Unmatched Images

# %%
# parameters

unmatched_widget_params = dict(
    view=...,
)

# %%
# call the task


unmatched_widget = (
    create_plot_widget_single_view.set_task_instance_id("unmatched_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(
        title="Unmatched Images", data=persist_unmatched_html, **unmatched_widget_params
    )
    .call()
)


# %% [markdown]
# ## Upload Images to EarthRanger Events

# %%
# parameters

upload_results_params = dict()

# %%
# call the task


upload_results = (
    upload_images_to_er_events.set_task_instance_id("upload_results")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(client=er_client_name, matched_df=matched_data, **upload_results_params)
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

persist_upload_results_params = dict()

# %%
# call the task


persist_upload_results = (
    persist_df.set_task_instance_id("persist_upload_results")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=upload_results,
        filetype="csv",
        filename="aerial_image_upload_results",
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_upload_results_params,
    )
    .call()
)


# %% [markdown]
# ## Create Dashboard

# %%
# parameters

dashboard_params = dict(
    groupers=...,
    warning=...,
)

# %%
# call the task


dashboard = (
    gather_dashboard.set_task_instance_id("dashboard")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        details=workflow_details,
        widgets=[matched_widget, unmatched_widget],
        time_range=time_range,
        **dashboard_params,
    )
    .call()
)
