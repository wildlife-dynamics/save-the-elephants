# AUTOGENERATED BY ECOSCOPE-WORKFLOWS; see fingerprint in README.md for details


# ruff: noqa: E402

# %% [markdown]
# # Aerial Lines
# TODO: top level description

# %% [markdown]
# ## Imports

import os

from ecoscope_workflows_core.tasks.analysis import (
    apply_arithmetic_operation as apply_arithmetic_operation,
)
from ecoscope_workflows_core.tasks.config import (
    set_workflow_details as set_workflow_details,
)
from ecoscope_workflows_core.tasks.filter import set_time_range as set_time_range
from ecoscope_workflows_core.tasks.groupby import set_groupers as set_groupers
from ecoscope_workflows_core.tasks.results import (
    create_single_value_widget_single_view as create_single_value_widget_single_view,
)
from ecoscope_workflows_core.tasks.results import gather_dashboard as gather_dashboard
from ecoscope_workflows_ext_ste.tasks import add_two_thousand as add_two_thousand

# %% [markdown]
# ## Set Workflow Details

# %%
# parameters

workflow_details_params = dict(
    name=...,
    description=...,
    image_url=...,
)

# %%
# call the task


workflow_details = (
    set_workflow_details.set_task_instance_id("workflow_details")
    .handle_errors()
    .with_tracing()
    .partial(**workflow_details_params)
    .call()
)


# %% [markdown]
# ## Time Range

# %%
# parameters

time_range_params = dict(
    since=...,
    until=...,
    timezone=...,
)

# %%
# call the task


time_range = (
    set_time_range.set_task_instance_id("time_range")
    .handle_errors()
    .with_tracing()
    .partial(time_format="%d %b %Y %H:%M:%S %Z", **time_range_params)
    .call()
)


# %% [markdown]
# ## Set Groupers

# %%
# parameters

groupers_params = dict(
    groupers=...,
)

# %%
# call the task


groupers = (
    set_groupers.set_task_instance_id("groupers")
    .handle_errors()
    .with_tracing()
    .partial(**groupers_params)
    .call()
)


# %% [markdown]
# ## Calculate Task

# %%
# parameters

calculator_params = dict(
    a=...,
    b=...,
    operation=...,
)

# %%
# call the task


calculator = (
    apply_arithmetic_operation.set_task_instance_id("calculator")
    .handle_errors()
    .with_tracing()
    .partial(**calculator_params)
    .call()
)


# %% [markdown]
# ## Add More

# %%
# parameters

add_more_params = dict()

# %%
# call the task


add_more = (
    add_two_thousand.set_task_instance_id("add_more")
    .handle_errors()
    .with_tracing()
    .partial(value=calculator, **add_more_params)
    .call()
)


# %% [markdown]
# ## Create Single Value Widgets

# %%
# parameters

sv_widgets_params = dict(
    view=...,
)

# %%
# call the task


sv_widgets = (
    create_single_value_widget_single_view.set_task_instance_id("sv_widgets")
    .handle_errors()
    .with_tracing()
    .partial(title="Sum", decimal_places=0, data=add_more, **sv_widgets_params)
    .call()
)


# %% [markdown]
# ## Create An Example Dashboard

# %%
# parameters

template_dashboard_params = dict(
    warning=...,
)

# %%
# call the task


template_dashboard = (
    gather_dashboard.set_task_instance_id("template_dashboard")
    .handle_errors()
    .with_tracing()
    .partial(
        details=workflow_details,
        widgets=sv_widgets,
        time_range=time_range,
        groupers=groupers,
        **template_dashboard_params,
    )
    .call()
)
