[project]
name = "ecoscope-workflows-ext-ste"
dynamic = ["version"]
requires-python = ">=3.10"
description = "Extension tasks and connections for the template repo."
readme = "README.md"
dependencies = [
    "wt-registry",
]
classifiers = [
    "Development Status :: 1 - Planning",
    "Operating System :: OS Independent",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering",
]
license = { text = "BSD-3-Clause" }
[project.entry-points."wt_registry"]
tasks = "ecoscope_workflows_ext_ste.tasks"


[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["ecoscope_workflows_ext_ste"]
exclude = ["vendor"]
only-packages = true
[tool.hatch.build.targets.sdist]
include = ["ecoscope_workflows_ext_ste"]
exclude = ["vendor"]
[tool.hatch.build.hooks.vcs]
version-file = "ecoscope_workflows_ext_ste/_version.py"
[tool.hatch.version]
source = "vcs"
fallback-version = "9999"
[tool.hatch.version.raw-options]
root = "../.."


[tool.pixi.workspace]
channels = [
    "https://prefix.dev/ecoscope-workflows-custom",
    "https://prefix.dev/ecoscope-workflows",
    "conda-forge",
]
platforms = ["linux-64", "osx-arm64", "win-64", "osx-64"]
channel-priority = "disabled"

[tool.pixi.pypi-dependencies]
ecoscope-workflows-ext-ste = { path = ".", editable = true }

[tool.pixi.dependencies]
# restating dependencies from ecoscope-workflows.core to ensure they are included
# todo: ensure exact 1:1 synchronization with ecoscope-workflows.core
# note: if depending on a conda release of ecoscope-workflows, restatement should not be necessary
# perhaps possible to demonstrate this here by making the restatement specific to a 'dev' environment
jinja2 = "*"
pandera = ">0.20.0"
pydantic = "<2.9.0"
pydantic-settings = "*"
py-rattler = ">=0.15.0,<0.16"
"ruamel.yaml" = "*"
ruff = "*"
tomli-w = "*"
geopandas="*"
shapely = "*"
# ecoscope-workflows-core = ">=0.22.18"
# ecoscope-workflows-ext-ecoscope=">=0.22.18"
# ecoscope-workflows-ext-big-life = "0.0.8.*"

ecoscope-workflows-ext-custom = "0.1.0rc14.*"

#ecoscope = { extras = ["analysis"], version = "*" }

docxtpl="*"
pillow ="*"
playwright="*"
docxcompose = "*"
earthengine-api ="*"
setuptools = "<=81.0.0"

# additional dependencies added by this extension
[tool.mypy]
plugins = ["pandera.mypy", "pydantic.mypy"]

[tool.pixi.feature.mypy]
dependencies = { mypy = "*", pandas-stubs = "*", pandera-mypy = "*" }
pypi-dependencies = { types-shapely = "*" }
tasks = { mypy = "mypy --package ecoscope_workflows_ext_ste --package tests --no-incremental" }

[tool.pixi.feature.py310.dependencies]
python = "3.10.*"
[tool.pixi.feature.py311.dependencies]
python = "3.11.*"
[tool.pixi.feature.py312.dependencies]
python = "3.12.*"
[tool.pixi.feature.test.dependencies]
pytest = "*"

[tool.pixi.environments]
default = { solve-group = "default" }

mypy-py310 = { features = ["test", "py310", "mypy"], solve-group = "py310" }
mypy-py311 = { features = ["test", "py311", "mypy"], solve-group = "py311" }
mypy-py312 = { features = ["test", "py312", "mypy"], solve-group = "py312" }

test-py310 = { features = ["test", "py310"], solve-group = "py310" }
test-py311 = { features = ["test", "py311"], solve-group = "py311" }
test-py312 = { features = ["test", "py312"], solve-group = "py312" }
